@model IEnumerable<ComercioApp.Models.Deposito>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodigoDeposito }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodigoDeposito }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodigoDeposito })
        </td>
    </tr>
}


</table>
<div>
    @Html.ActionLink("Ir al formulario Productos", "Index", "Producto")
</div>